---
import DonationCardComponent from './DonationCardComponent.astro';
import { LockSection } from './LockSection';

export interface CardProps {
  currentAmount: number;
  startAmount: number;
  endAmount: number;
  peopleGroups: number;
  projectHeader: string;
  projectDescription: string;
  projectBullets: string[];
  image: string;
}

interface Props {
  projectData: CardProps[];
}

const { projectData } = Astro.props;
---

{projectData.map((data, index) => <DonationCardComponent projectData={{ ...data, index }} />)}
<div class="donation-card donation-card-grey w-full border-gray-300">
  <div class="donation-card-image-wrapper w-full relative">
    <div class="relative">
      <img
        class={'more-to-come-card-image grayscale overflow-hidden'}
        src="https://cdn.sanity.io/media-libraries/ml0ZDygBMJD9/images/cba77b66fe7213db90c3f65ced2f34551c5af41f-1200x675.jpg"
        alt=""
      />
      <span class="more-to-come-card absolute inset-0 top-1/2">JUST THE BEGINNING</span>
    </div>
    <div class="relative donation-card-content">
      <div class="donation-card-subheading mt-2">
        JUST THE BEGINNING
      </div>
      <div class="lg:flex justify-between gap-2">
        <div class="donation-card-heading">
          {`Future Opportunity`}
        </div>
        <div class="flex items-center">
          <LockSection client:load fundingStatus="locked" />
        </div>
      </div>
      <p class="donation-card-description px-5">
        The work is not finished! We are expectant to see how God will use this effort in the years
        to come as we continue putting new translation projects before our church body. May the Lord
        open doors for even more people to experience His Word in their own language.
      </p>
    </div>
  </div>
</div>
